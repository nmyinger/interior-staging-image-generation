# Bug Log

Bugs found during development, to be fixed in a future session.

| # | Area | Description | Priority | Status |
|---|------|-------------|----------|--------|
| 1 | Properties | No option to remove an uploaded photo from a property | Medium | Fixed |
| 2 | Properties / Canvases | Card "Open" button looks unprofessional; entire card should be clickable to open (affects both property cards and canvas cards inside a property) | Medium | Fixed |
| 3 | Properties | "Stage all photos" does not automatically create a canvas for the staged results | High | Fixed |
| 4 | Properties | Property cards on the properties list show no photo thumbnails; should preview a few example photos from the property | Medium | Fixed |
| 5 | Properties | Property detail page is disorganized — photos should be grouped by room and split into unfurnished vs. furnished columns; remove the non-functional "Draft" status tag | High | Fixed |
| 6 | Properties / Rooms | Dragging an already-uploaded photo image triggered the file-upload handler, causing a re-upload | High | Fixed |
| 7 | Properties / Rooms | Photo delete button was hidden behind a hover overlay — the last photo in a room couldn't reliably be deleted | Medium | Fixed |
